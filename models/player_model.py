class PlayerModel:
    def __init__(self, player_id, nickname, is_bot, score, game_id):
        self.player_id = player_id
        self.nickname = nickname
        self.is_bot = is_bot
        self.score = score
        self.game_id = game_id
